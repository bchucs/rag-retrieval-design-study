# Experiment 2: Top-k and distance threshold variations
# Tests: top_k (3/5/10) and distance cutoff strategies

experiments:
  - name: "topk_3_no_threshold"
    retrieval:
      top_k: 3
      distance_threshold: null

  - name: "topk_5_no_threshold"
    retrieval:
      top_k: 5
      distance_threshold: null

  - name: "topk_10_no_threshold"
    retrieval:
      top_k: 10
      distance_threshold: null

  - name: "topk_5_threshold_strict"
    retrieval:
      top_k: 5
      distance_threshold: 0.5  # adjust based on embedding model

  - name: "topk_10_threshold_lenient"
    retrieval:
      top_k: 10
      distance_threshold: 0.7